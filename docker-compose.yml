version: '3.9'

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfilea
    ports:
      - "3000:3000"

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ./backend/clicks.db:/app/clicks.db 

  plugin:
    build:
      context: ./plugins
      dockerfile: Dockerfile
    ports:
      - "50001:50001"

  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - frontend
      - backend